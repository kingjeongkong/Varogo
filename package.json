{
  "name": "varogo",
  "private": true,
  "scripts": {
    "dev:backend": "cd apps/backend && poetry run uvicorn app.main:app --reload --port 3000",
    "dev:frontend": "pnpm --filter frontend dev",
    "build:frontend": "pnpm --filter frontend build"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "@prisma/client",
      "@prisma/engines",
      "@swc/core",
      "bcrypt",
      "prisma",
      "unrs-resolver"
    ]
  }
}
